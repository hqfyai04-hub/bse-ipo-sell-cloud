{
  "name": "bse-ipo-sell-cloud",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "description": "北交所新股首日卖出窗口助手 PWA 与 Android 客户端",
  "scripts": {
    "web:build": "node scripts/build-mobile.mjs",
    "pwa:check": "node scripts/check-pwa.mjs",
    "android:add": "npm run web:build && cap add android",
    "android:sync": "node scripts/build-mobile.mjs --strict && cap sync android",
    "android:open": "npm run android:sync && cap open android",
    "android:debug": "npm run android:sync && node scripts/run-gradle.mjs assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
