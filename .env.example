# 留空表示公开访问；建议公网部署时设置高强度随机口令。
APP_ACCESS_TOKEN=
# 设置为 1 才开放 /api/docs。
ENABLE_API_DOCS=0
# 额外允许访问 API 的网页来源，多个来源用逗号分隔。安卓默认的
# https://localhost 已内置，无需重复填写。
APP_ALLOWED_ORIGINS=
# 仅用于生成 APK 前端资源；必须是已部署后端的 HTTPS 地址。
APP_API_BASE_URL=
